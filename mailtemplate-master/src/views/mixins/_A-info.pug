mixin a-info(data)
    a(href=data.link target="_blank" xt="SPCLICKSTREAM" name=data.link).tileLink
        img.topImage(src=`assets/img/mail/${data.image}.jpg` name=`${data.image}`)
        wrapper.tile
            center
                span.spacer
                p= data.categorie1
                wrapper.btn.blue
                        p.centerText= data.cta  
                            span.test= " ›"